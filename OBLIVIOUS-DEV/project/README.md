# `OBLIVIOUS-DEV/project`

**Canonical sources are not duplicated here.** They live at the
workspace root next to `OBLIVIOUS-DEV/`:

- `../oblivious-hub/` — Electron hub
- `../bookmap-plugin/` — Bookmap Java plugin
- `../mql-zmq-bundle/` — MT4 ZMQ runtime bundle
- `../OBLIVIOUS_COMPLETE.mq4`, `../COMPILA.bat` — EA build

This folder holds only:

| Subfolder | Contents |
|-----------|----------|
| `bridge/` | Placeholder README — no live C++ bridge in current architecture |
| `legacy-frozen/` | Archived `.NET` / named-pipe stack (**obsolete**, reference only) |

Keeping one copy of sources avoids drift between “root” and “DEV mirror”.
